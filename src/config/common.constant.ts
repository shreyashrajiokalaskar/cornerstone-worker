export enum DOC_STATUS {
    PENDING = 'PENDING',
    PROCESSING = 'PROCESSING',
    COMPLETED = 'COMPLETED',
    FAILED = 'FAILED',
}

export enum AuthProvider {
    EXTERNAL = 'EXTERNAL',
    INTERNAL = 'INTERNAL',
}
